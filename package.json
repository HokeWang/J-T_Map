{
  "name": "address-geocoding-comparison",
  "version": "0.1.0",
  "private": true,
  "description": "Compare Google geocoding results across address input schemes.",
  "scripts": {
    "build": "node build.js",
    "start": "node server.js"
  }
}